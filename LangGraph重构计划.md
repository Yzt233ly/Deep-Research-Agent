# LangGraph 重构学习计划：从手写流程到图编排

> 更新日期：2026-09-11。本文是待实施的教学计划，不代表重构已经完成。
> 学习主线：先看懂自己写过的 Python 流程，再用 LangGraph 表达同一个流程。
> 最终目标：能独立解释并运行 LangGraph 版 Deep Research Agent，保留当前 Streamlit 页面和人工计划确认，作为本项目的收官成果。

## 一、这次究竟要学什么

你已经实现了规划、ReAct、搜索、抓取、压缩、反思、报告和人工确认。这次不再增加研究能力，而是用熟悉的功能学习一种新的组织方式。

原来由方法调用、for 循环和 if 分支安排研究步骤；现在由 LangGraph 的状态、节点和边安排同样的步骤。

| 概念 | 大白话解释 | 对应旧代码 |
| --- | --- | --- |
| 状态（State） | 记录研究到了哪里、收集到了什么 | observations、local_notes、answers 等变量 |
| 节点（Node） | 一次负责一个明确步骤 | 规划、思考、搜索、反思等方法 |
| 边（Edge） | 决定完成一步后去哪里 | 方法调用顺序 |
| 条件边（Conditional Edge） | 根据结果选择路线 | search / finish 和反思分支 |
| 图的构建与运行 | 把步骤连接起来，再交给框架执行 | 原来的主流程方法 |
| 中断与恢复（Interrupt / Resume） | 暂停等人确认，然后继续 | 网页生成计划与执行之间的等待 |
| 检查点（Checkpoint） | 保存图的进度，供恢复时使用 | 支撑人工审批的状态保存 |

这些概念按步骤引入，不要求开始前全部学会。完成标准是：你知道代码为什么这样写，并能修改一条分支后预测执行结果。

## 二、后续教学如何进行

每一步固定围绕五件事：

1. **学什么**：只介绍本步骤需要的新概念，首次出现给英文原文和中文解释。
2. **对照哪里**：先读一小段旧代码，看现在是谁保存数据、谁决定下一步。
3. **动手改什么**：写一小块可运行代码，中文注释解释作用、原因和容易踩的坑。
4. **运行看什么**：使用假数据观察执行顺序和状态变化，先懂机制，再接真实工具。
5. **怎样自检**：用一两个问题验证理解，并结合运行结果解释答案。

不一次性生成全部代码后让你自行消化。每一步结束说明改了哪些文件、理解了什么、下一步连接哪里。自检用于帮助理解，不作为反复确认的审批手续。

遇到 Python 语法障碍，例如 TypedDict、类型注解、闭包，先在对应位置解释，不另外展开一套无关课程。

## 三、明确控制范围

### 复用已有成果

| 模块 | 本次处理 |
| --- | --- |
| src/llm/client.py | 保留 DeepSeek、结构化输出、重试和费用统计 |
| src/tools/ | 保留 Tavily、抓取、来源登记和模拟搜索 |
| compressor.py、reflector.py | 保留压缩和反思能力 |
| src/models/schemas.py | 复用现有 Pydantic 模型 |
| src/agent/researcher.py | 作为主要学习对照，逐段迁移流程 |
| app.py、src/ui/support.py | 保留页面、计划编辑和结果展示，最后接入图 |
| tests/helpers.py | 复用 FakeLLMClient、FakeSearchTool、FakeFetcher |

保留优先级、整体研究思路、轮数限制、来源编号、报告下载和 Observation 日志换行。不更换模型，不重新设计网页。

### 不列为本次要求

- 并行研究、多 Agent、子图和复杂归并器。
- 数据库检查点、服务重启恢复和任务历史管理。
- 从失败节点精确重试、外部 API 恰好调用一次等可靠性机制。
- 通用服务层、后台任务系统、复杂日志队列和并发压测。
- 新增 RAG 知识库、账号系统或云部署。

这些不进入最终验收，也不成为必须补做的后续版本。

仍保留基本正确性：非法输入提示、达到上限结束、普通页面交互不重复调用模型、新研究不串用旧计划和来源。这些是当前功能正常工作的必要条件。

## 四、步骤 1：先运行一张最小的图

**学什么：** State、Node、普通边、START、END、compile 和 invoke。

**对照哪里：** 旧代码中“先调用一个方法，再调用另一个方法”的顺序，暂时不进入 ReAct。

### 动手改什么

写一个独立的教学示例，不调用模型、不联网：

```text
START → 准备主题 → 生成固定结论 → END
```

状态先只有 topic 和 summary。节点就是普通 Python 函数：读取状态，返回需要更新的字段。示例保持几十行，不接 Streamlit。

沿着代码解释：StateGraph 描述结构，add_node 注册步骤，add_edge 连接步骤，compile 得到可执行图，invoke 运行并取得结果。

先采用默认覆盖更新，不引入 Annotated、operator.add 等归并器。节点不必每次返回完整状态，只返回要更新的字段。[官方 Graph API](https://docs.langchain.com/oss/python/langgraph/graph-api)

### 运行与自检

- 修改 topic，观察每个节点读到什么以及最终结果。
- 让一个节点只更新 summary，观察 topic 是否仍然保留。
- 自检：compile 和 invoke 有什么区别？为什么注册节点后还需要连接边？

**完成标志：** 能解释执行顺序，并自己增加一个简单节点。

## 五、步骤 2：把一次 ReAct 决策改成条件边

**学什么：** 条件边、回边，以及图如何表达循环。

**对照哪里：** researcher.py 的 _run_react() 中 action.action 的判断。

### 动手改什么

只研究一个子问题，先使用固定假动作：

```text
思考 → search → 模拟搜索 → 回到思考
     → finish → 结束
```

按需增加 action、observations、iteration 字段。条件函数只负责选择路线，不调用模型或执行搜索。

先模拟“搜索一次然后结束”，再加入次数限制，让一直返回 search 的情况也能结束。

### 需要看懂的变化

原来 for 负责重复、if 负责选择；现在回边负责重复、条件边负责选择。业务上最多研究几轮仍需要自己定义。

图的 recursion_limit 是图执行步数限制，不等于 MAX_ITERATIONS：一轮研究可能经过多个节点。设置够用的图步数预算并测试即可，不额外开发预算系统。

### 运行与自检

- 直接 finish：不执行搜索。
- search 后 finish：只搜索一次。
- 一直 search：达到上限后收尾。
- 自检：图里的循环在哪里？去掉回边会发生什么？

**完成标志：** 能画出三条路径，说明条件边怎样替代旧分支。

## 六、步骤 3：接回工具、压缩和反思

**学什么：** 节点如何复用业务代码，状态如何在节点间传递资料。

**对照哪里：** _web_search()、反思分支、_answer_from_notes()、_force_answer()。

### 动手改什么

接回已有依赖，自动化测试仍然传入 Fake 对象：

```text
思考 → 搜索、去重、抓取、压缩 → 反思
                              ├─ 足够：基于事实作答
                              ├─ 不足且还有次数：回到思考
                              └─ 不足且达到上限：依据已有信息收尾
```

搜索、去重、抓取和压缩先合在一个“收集资料”节点，沿用熟悉的调用顺序。无需为了使用框架把每一小步都拆成节点。

客户端和工具通过构建函数或简单对象传入。State 保存研究数据，不保存客户端、logger 或网页控件；继续使用已有依赖注入方式，不建设额外框架。

### 此时理解哪些状态

| 字段 | 用途 |
| --- | --- |
| question | 当前问题 |
| iteration | 已进行的决策轮数 |
| action | 本轮动作 |
| observations | 下一轮需要看到的资料与反思建议 |
| local_notes | 当前问题的关键事实 |
| reflection | 用来判断下一步的反思结果 |
| answer | 当前问题的答案 |

字段按需增加，解释谁写入、谁读取、什么时候重置，不要求机械照抄表格。

### 运行与自检

- 用假数据分别触发“反思足够”和“反思不足”。
- 检查不足时的建议是否进入下一轮输入。
- 确认重复来源仍被去重，抓取失败仍使用搜索摘要。
- 自检：下一轮为什么知道上一轮搜到了什么？如果 observations 被清空会怎样？

**完成标志：** 单个子问题能够完成原有研究过程，你能解释资料在 State 中怎样传递。

## 七、步骤 4：从一个子问题扩展为完整研究

**学什么：** 全局状态与局部状态，图怎样替代外层子问题循环。

**对照哪里：** research_from_plan() 中的子问题遍历、答案收集和报告生成。

### 动手改什么

在单题流程周围增加计划、准备当前问题、收集答案、切换问题和写报告：

```text
生成计划 → 准备当前问题 → 单题研究 → 收集答案
                ↑                      │
                └──── 还有问题 ────────┤
                                       └─ 全部完成 → 写报告 → END
```

单题流程保留上一阶段的显式节点，不把整个旧 _run_react() 塞进一个黑盒节点。

逐步加入 plan、当前问题索引、全部答案、全部事实和 sources。用两个子问题观察：

- 换题时清空 observations、local_notes 和当前轮数。
- 已完成答案和全局来源保留。
- 优先级降序，同优先级保留人工计划顺序。
- 整体研究思路继续影响执行和报告。

### 为什么要处理来源状态

原来来源存在 self.registry 中。迁移时以图状态中的来源列表为依据，登记时复用 SourceRegistry 并保持编号顺序。这是学习“哪些数据属于任务”，不扩展成数据库或历史恢复系统。

State 先用 TypedDict 声明；输入校验复用 Pydantic。讲清楚 TypedDict 主要提供类型描述，本身不执行运行时校验。节点显式返回新的列表值，避免悄悄修改共享对象。

保持 topic / plan / answers / notes / sources / report 的输出结构，减少页面改动。参考文献仍由程序根据来源列表生成，不交给模型编造；来源可追溯不等于结论已核实。

### 运行与自检

- 第二题轮数从头开始，第一题答案仍存在。
- 两题出现同一来源时，引用和去重保持一致。
- with_report=False 时不调用报告模型。
- 自检：为什么 local_notes 换题要清空，sources 却应该保留？

**完成标志：** 图能完成一份研究，你能指出原来内外两层循环分别被哪些边替代。

## 八、步骤 5：用图中断实现人工计划确认

**学什么：** interrupt、Command(resume=...)、内存检查点和 thread_id。

**对照哪里：** 页面分别调用 make_plan() 和 research_from_plan()，在中间等人确认。

### 动手改什么

规划后增加独立的审批节点：

```text
生成计划 → 审批节点暂停 → 人工提交修改后的计划 → 开始研究
```

先不接网页，在示例或测试中执行两次调用：第一次运行到暂停，拿到计划；第二次提交人工版本，让研究继续。

使用 InMemorySaver。每个新研究分配独立 thread_id，恢复时沿用原标识。它是查找任务进度的标识，不是 Python 线程编号。

恢复会重新进入含中断的节点，因此规划和审批分开，审批节点中不放付费模型调用，避免确认时再次生成计划。[官方 Interrupts 文档](https://docs.langchain.com/oss/python/langgraph/interrupts)

### 必须看懂的细节

- interrupt 让图暂停，不是在函数中用 input() 一直等待。
- Command(resume=...) 提交的内容成为中断调用的返回值。
- 图对象和检查点要保留，不能恢复时重新创建而丢失进度。
- 人工内容仍校验空计划、空问题和优先级。
- 不用普通异常捕获吞掉图中断的控制信号。
- 内存检查点不承诺服务重启恢复；本次只学习审批暂停与继续。

### 运行与自检

- 暂停后搜索调用次数为零。
- 修改、删除、补充子问题后恢复，执行人工版本。
- 恢复后规划调用次数仍为一次。
- 自检：恢复时换 thread_id 为什么不能接着原计划执行？为什么不能把规划调用放到审批节点开头？

**完成标志：** 没有 Streamlit 也能证明图完成“暂停 → 接收人工输入 → 继续”。

## 九、步骤 6：接回 Streamlit，完成项目

**学什么：** 网页状态和图状态各管什么，怎样把跑通的图接到当前界面。

**对照哪里：** app.py 的生成、确认、结果展示和 session_state。

### 动手改什么

- “生成计划”运行图到审批中断。
- 继续用现有表格编辑计划。
- “确认并开始研究”将校验过的计划交给图恢复。
- 图、检查点和任务 ID 保存在当前 session_state 中。
- 复用报告、来源、事实、答案、费用和 Markdown 下载展示。

页面状态负责展示哪些控件和表格修改，图状态负责研究进度和资料。避免两边分别维护一套独立的研究进度。

先用简单调用方式跑通，再通过 stream(..., stream_mode="updates") 观察节点完成信息。只适配一种经过验证的返回格式，不建立通用事件系统。[官方 Streaming 文档](https://docs.langchain.com/oss/python/langgraph/streaming)

### 日志和异常保持简单

保留现有日志，先验证哪些部分能复用。当前日志按线程筛选，如果图的执行线程不同，针对实际问题调整；不提前强制消息队列或 ContextVar 架构。页面渲染留在 Streamlit 执行上下文，不在工作线程随意调用 st.*。

保留日志数量上限、Key 脱敏和 Observation 后换行。出错时显示原因，允许重新发起研究，不要求从失败节点精确恢复。普通页面重跑、查看和下载不得重复调用模型。

命令行继续一键研究：由入口在图暂停后自动确认计划，复用同一张图；网页仍等待人确认。

### 运行与自检

- 生成计划后页面可编辑，研究尚未开始。
- 确认后执行人工版本，完成页显示对应结果。
- 普通 rerun 不重新规划或搜索，换主题不沿用旧任务。
- 自检：为什么只保存计划还不够，还需要保存图、检查点和任务 ID？

**完成标志：** 现有操作完整保留，你能从页面按钮一路解释到图节点。

## 十、必要验证，不另造测试工程

每一步只写能证明当前机制的必要测试，继续复用 Fake/Mock，不凑数量、不做并发压测。

| 测试 | 要证明的内容 |
| --- | --- |
| search / finish | 条件边正确 |
| 反思足够 / 不足 | 资料影响路线 |
| 达到上限 | 循环能够结束 |
| 两个子问题 | 局部重置、全局保留正确 |
| 审批前不搜索、恢复不重规划 | 中断与恢复正确 |
| 人工修改实际生效 | HITL 不只是显示表格 |
| 来源和报告 | 现有结果能力保留 |
| 页面重跑 | 不重复执行研究 |

保留原有工具、压缩、反思测试。接口改变时更新测试，不强求每个旧私有方法原封不动保留。

默认执行 `.venv\Scripts\python.exe -m pytest -q`，不联网、不调用收费 API。最后用一个小主题手动验证真实调用、网页和下载，记录实际结果；外部服务不可用时如实说明，不能把离线测试当成真实链路已通过。

## 十一、文件与 GitHub 安排

### 文件按学习需要增加

先写一个小教学示例。迁移真实流程时优先使用少量文件，例如：

```text
src/agent/graph_state.py        # 状态定义
src/agent/graph_researcher.py   # 节点、路由、构图和简单调用入口
```

内容变长影响阅读后再拆节点模块。不提前要求六个模块、service 层或通用接口。教学示例是否保留，以是否帮助理解为准。

安装 LangGraph 时选择与当前环境兼容且测试通过的版本，不顺便升级全部依赖。详细写法以选定版本的官方文档为准。

### 使用原 GitHub 仓库

1. 开始前提交可运行的手写版，记录测试基线。
2. 用 v1.0-manual 标签保留手写版。
3. 在 refactor/langgraph 分支学习和实现重构。
4. 完成后合并 main，用 v2.0-langgraph 标记收官成果。
5. README 提供手写版入口、新版运行方法和概念对照。

同名标签已存在时核实而不覆盖。这些只是计划，本次文档修改不执行 Git 操作。两个标签保存重构前后成果，不代表还要开发更多产品版本。

## 十二、最终完成清单

### 学习上

- [ ] 能解释 State，以及哪些节点读写哪些字段。
- [ ] 能解释节点、边和条件边，并画出研究流程。
- [ ] 能指出原来两层循环在图中如何实现。
- [ ] 能解释局部状态重置与全局来源保留。
- [ ] 能解释中断、恢复、检查点和 thread_id 的关系。
- [ ] 能修改一条简单路线，用假数据验证自己的预测。

### 功能上

- [ ] LangGraph 真正控制规划、研究循环与报告。
- [ ] 确认前不搜索，确认后执行人工计划。
- [ ] 原有搜索、抓取、压缩、反思、引用和次数限制保留。
- [ ] Streamlit 和命令行可用，报告可查看和下载。
- [ ] 必要测试通过，真实验证有记录。
- [ ] GitHub 保留手写版快照，README 说明新旧差别。

**达到这些标准，本项目就完成。无需增加数据库、并发系统或复杂故障恢复来证明学会了 LangGraph。**
