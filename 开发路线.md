# Deep Research Agent 开发路线

> 学习策略：**先用原生 Python + LangChain 手写一遍（理解原理），再用 LangGraph 重构一遍（理解编排）。**
>
> 本路线按"能跑通、能理解、能展示"的顺序，把整个项目拆成 6 个步骤，每个步骤结束都有一句「完成标志」，帮你判断是否真的学会了。

---

## 总览

| 步骤 | 名称 | 主要知识点 | 对应批次 |
| --- | --- | --- | --- |
| 1 | 项目骨架与工程化基础 | 目录结构、配置管理、日志 | 第一批 |
| 2 | LLM 封装层 | 重试、Token 统计、类型注解 | 第一批 |
| 3 | 数据模型与结构化输出 | Pydantic、Structured Output | 第一批 |
| 4 | ReAct 循环核心 | ReAct、Planning、任务分解 | 第一批 |
| 5 | 搜索、抓取与引用溯源 | Tool Calling、Web Scraping、引用 | 第二批 |
| 6 | Agent 智能化 + 打磨发布 | 上下文压缩、反思、去重、测试、README | 第二、三批 |

> 说明：异步并发、缓存、Human-in-the-Loop 属于进阶项，会在对应步骤里标注为「可选」，不单独设步骤，避免贪多嚼不烂。

---

## 步骤 1：项目骨架与工程化基础

### 目标
先把"房子"的框架搭起来，让后面每一步的代码都有正确的地方放。

### 要做的事
- 建立清晰的目录结构（`src/` 分层：agent、tools、llm、models、utils）
- 创建 `requirements.txt`、`.gitignore`、`.env.example`、`.env`
- 用 `pydantic-settings` 写一个 `Settings` 配置类，统一读取 API Key、模型名等
- 用 `logging` 替代 `print`，写一个统一日志格式

### 涉及知识点
- 项目目录结构设计（为什么分层）
- 配置管理：环境变量与 `.env` 文件
- 日志系统：`logging` 的级别与格式
- 依赖管理：`requirements.txt`

### 完成标志
运行 `python -m src.main`，能在终端看到一条带时间戳的日志：`[INFO] 配置加载成功，模型：gpt-4o-mini`。

---

## 步骤 2：LLM 封装层

### 目标
把调用 LLM 的逻辑封装成一个类，加入重试和成本统计，避免以后每处都裸调 API。

### 要做的事
- 封装一个 `LLMClient` 类，统一负责调用模型
- 用 `tenacity` 给 LLM 调用加重试（失败自动重试 3 次）
- 每次调用后记录 `input_tokens`、`output_tokens`、估算费用，并打印到日志
- 给所有函数参数和返回值加类型注解

### 涉及知识点
- 重试机制（`tenacity`）
- Token 与成本管理
- 类型注解（Type Hints）

### 完成标志
故意传一个错误的 API Key，能看到日志里出现「第 1 次重试…第 2 次重试…」；传正确 Key 后，能看到本次调用消耗的 token 和费用。

---

## 步骤 3：数据模型与结构化输出

### 目标
定义整个 Agent 用到的数据结构，并让 LLM 能输出严格符合这些结构的内容。

### 要做的事
- 用 Pydantic 定义模型：
  - `SubQuestion`（子问题 + 优先级）
  - `ResearchPlan`（子问题列表 + 整体思路）
  - `SearchQuery`（搜索关键词 + 意图）
  - `ResearchNote`（关键事实 + 来源）
  - `Citation`（标题、URL、摘要）
- 用 LangChain 的 `with_structured_output` 让 LLM 按这些模型输出
- 处理解析失败的情况（输出不符合格式时怎么办）

### 涉及知识点
- Pydantic 数据校验
- Structured Output（结构化输出）
- 输出解析与容错

### 完成标志
给 LLM 一句话"帮我研究 AI Agent 趋势"，它能返回一个合法的 `ResearchPlan` 对象，里面正确包含多个 `SubQuestion`。

---

## 步骤 4：ReAct 循环核心

### 目标
手写 Agent 的"大脑"——ReAct 循环，让 AI 能"边想边做"，这是整个项目的灵魂。

### 要做的事
- 理解 ReAct = Reasoning（思考）+ Acting（行动），循环结构为 `Thought → Action → Observation`
- 先让 LLM 制定研究计划（Plan），再拆成子问题（Task Decomposition）
- 手写循环：对每个子问题 → 搜索 → 观察结果 → 判断是否继续
- 设置最大循环次数，防止死循环

### 涉及知识点
- ReAct 推理模式
- Planning（规划）
- Task Decomposition（任务分解）
- 循环终止条件

### 完成标志
输入"分析 2026 年 AI Agent 发展趋势"，Agent 能自主生成子问题清单，并按顺序打印每一步的 Thought/Action/Observation 过程。

---

## 步骤 5：搜索、抓取与引用溯源

### 目标
让 Agent 真正"看到"外部世界的信息，并把信息变成可信、可追溯的引用。

### 要做的事
- 接入一个搜索工具（Serper / Tavily / DuckDuckGo 等，选一个免费的）
- 写网页正文抓取函数（`requests` + `BeautifulSoup` 或 `trafilatura`）
- 把每个搜索结果整理成 `Citation`（标题、URL、摘要），保存下来
- 让最终报告对每个结论标注 `[1]` `[2]`，并附参考文献列表

### 涉及知识点
- Tool Calling（搜索工具）
- Web Scraping（网页正文提取）
- Source Citation（引用与来源溯源）

### 完成标志
Agent 能对某个子问题真正搜索并抓取网页正文，最终报告里每个关键论断都带 `[n]` 编号，且能对应到文末的参考文献。

---

## 步骤 6：Agent 智能化 + 打磨发布

### 目标
让 Agent 从"能用"变成"好用"，然后把项目整理成能放进 GitHub 的样子。

### 要做的事
- 上下文压缩：每篇文章只提取 3~5 条关键事实存进 `ResearchNote`，不塞全文
- 反思（Reflection）：搜索后让 LLM 判断"资料够不够"，不够就换关键词再搜
- 简单去重：标题相似或 URL 相同的搜索结果跳过
- 写单元测试（`pytest` + Mock 模拟 LLM，不真调 API）
- 写 `README.md`，说明项目是什么、怎么跑、技术栈
- 用 Git 分步提交，写规范的 commit message

### 涉及知识点
- Context 压缩（上下文窗口管理）
- Reflection（自评估）
- 去重
- 单元测试 + Mock
- README 与 Git 最佳实践

### 可选进阶（学有余力再加）
- 异步并发搜索（`asyncio` + `gather`）
- 搜索结果缓存（本地 JSON 或 SQLite）
- Human-in-the-Loop（生成计划后让用户确认）

### 完成标志
输入一个研究主题，Agent 能自主完成"规划 → 搜索 → 抓取 → 压缩 → 反思 → 再搜索 → 生成带引用的报告"，且全程日志清晰；`pytest` 测试全部通过；GitHub 上能看到一个结构清晰、有 README 的项目。

---

## 做完之后：用 LangGraph 重构

当你用原生 Python + LangChain 把上面 6 步走通后，再回过头用 **LangGraph** 把"搜索 → 判断 → 再搜索 → 生成报告"这条流程用 **图（State / Node / Edge）** 重新表达一遍。

这时你会很自然地理解：

- 原来手写循环里的 `if/else/while`，就是 LangGraph 里的 **Conditional Edge**
- 原来分散在代码里的变量，就是 LangGraph 里的 **State**
- 原来每一步的操作，就是 LangGraph 里的 **Node**

> 一句话：**先懂原理，再学工具，你会知道"为什么需要 LangGraph"，而不是只会调它的 API。**
